Function DAG for maps/erosion/index.html
Generated 2026-05-29 from the inline widget script.

Summary:
- No disconnected function family was found.
- Roots are page-load initialization, browser error handling, file upload, buffer selection, surface-option controls, erosion controls, reset buttons, and download buttons.
- Hidden controls still root resetFadeSlopeParameters and resetPhaseParameters through their reset buttons.

Page-load and global roots:
- window error event
  -> reportError
    -> setProgress
    -> logDiagnostics
- initial script execution
  -> updateSurfaceOptionVisibility
  -> updateDefaultHeightVisibility
  -> normalize3, through SIMPLE_RENDER_LIGHT initialization

Upload and full pipeline root:
- fileInput change
  -> runPipeline
    -> resetPipelineState
      -> setDownloadButtonsEnabled
    -> setProgress
    -> loadImageFromFile
    -> drawImageToCanvas
      -> setCanvasSize
    -> selectBuffer
      -> findBufferView
      -> updateBufferSelection
      -> drawSelectedBufferPreview
        -> findBufferView
        -> setCanvasSize
    -> imageDataToGrayscale
    -> collectDenseImagePoints
    -> readSurfaceParameters
    -> reconstructSurface
      -> reconstructGaussianScaleSpaceSurface, when Gaussian scale-space is selected
      -> reconstructRelaxedBSplineSurface, otherwise
    -> findMaxAbs
    -> paintFloatChannel
      -> setCanvasSize
    -> paintHeightAndSlope
      -> setCanvasSize
      -> clamp01
        -> clamp
    -> paintGradientField
      -> setCanvasSize
      -> clamp01
        -> clamp
    -> rememberFittedSurface
      -> findMinMax
    -> renderErosionOutputsFromState
    -> formatPipelineStatus
    -> logDiagnostics
  -> reportError, on thrown errors

Surface reconstruction dispatch:
- reconstructSurface
  -> reconstructRelaxedBSplineSurface
    -> fitMultilevelBSpline
    -> evaluateBSplineHeightAndSlope
  -> reconstructGaussianScaleSpaceSurface
    -> runWebGLGaussianDerivativeSurface

Relaxed B-spline surface fitting:
- collectDenseImagePoints
- reconstructRelaxedBSplineSurface
  -> fitMultilevelBSpline
    -> nextFrame
    -> fitSplineLevel
      -> relaxedAnchorResidual
      -> writeCubicBasis
    -> evaluateSplineLevelAtPoint
      -> writeCubicBasis
  -> evaluateBSplineHeightAndSlope
    -> precomputeSplineAxis
      -> writeCubicBasis
      -> writeCubicBasisDerivative
      -> writeCubicBasisSecondDerivative
    -> nextFrame

WebGL Gaussian scale-space surface fitting:
- reconstructGaussianScaleSpaceSurface
  -> runWebGLGaussianDerivativeSurface
    -> buildGaussianDerivativeKernels
    -> setProgress
    -> nextFrame
    -> grayToUnitHeightValues
    -> unitHeightValuesToSourceData
    -> createFloatTexture
    -> createKernelTexture
      -> createFloatTexture
    -> createProgram
      -> compileShader
    -> buildGaussianHorizontalSource
    -> buildGaussianVerticalSource
    -> renderFullScreenPass
      -> createFramebufferForTexture
    -> createFramebufferForTexture

RuneVision erosion shader and WebGL erosion pass:
- renderErosionOutputsFromState
  -> renderErosionPass, once for each erosion output canvas
    -> setCanvasSize
    -> createHeightAndSlopeTexture
    -> createProgram
      -> compileShader
    -> fullScreenTriangleVertexSource
    -> runeVisionFragmentSource
      -> GLSL encodeGradient
      -> GLSL hash
      -> GLSL pow_inv
      -> GLSL ease_out
      -> GLSL smooth_start
      -> GLSL safe_normalize
      -> GLSL PhacelleNoise
      -> GLSL ErosionFilter

Erosion output orchestration:
- renderErosionOutputsFromState
  -> readErosionParameters, when called without explicit parameters
  -> requireFittedSurface
  -> renderErosionPass
  -> decodeErodedHeight
  -> paintSimpleRender
    -> setCanvasSize
    -> simpleBumpShade
      -> sampleHeight
      -> clamp01
        -> clamp
    -> simpleRenderColor
      -> clamp01
        -> clamp
    -> clamp
  -> setDownloadButtonsEnabled
  -> drawSelectedBufferPreview
    -> findBufferView
    -> setCanvasSize

Surface option control roots:
- surfaceTypeInput change
  -> rerenderSurfaceFromControls
    -> updateSurfaceOptionVisibility
    -> collectDenseImagePoints
    -> setProgress
    -> readSurfaceParameters
    -> reconstructSurface
    -> findMaxAbs
    -> paintFloatChannel
    -> paintHeightAndSlope
    -> paintGradientField
    -> rememberFittedSurface
    -> renderErosionOutputsFromState
    -> formatPipelineStatus
    -> logDiagnostics
    -> reportError, on thrown errors
- gaussianSigmaInput input
  -> updateParameterOutput
    -> formatParameterValue
- gaussianSigmaInput change
  -> rerenderSurfaceFromControls

Erosion parameter control roots:
- each erosionParameterInputs input event
  -> updateParameterOutput
    -> formatParameterValue
    -> updateDefaultHeightVisibility, only for fadeTargetModeInput
- each erosionParameterInputs change event
  -> updateParameterOutput
  -> rerenderErosionFromControls
    -> setProgress
    -> renderErosionOutputsFromState
    -> formatPipelineStatus
    -> logDiagnostics
    -> reportError, on thrown errors

Reset-button roots:
- resetErosionButton click
  -> resetErosionParameters
    -> resetParameterInputs
- resetOnsetButton click
  -> resetOnsetParameters
    -> resetParameterInputs
- resetFadeSlopeButton click
  -> resetFadeSlopeParameters
    -> resetParameterInputs
- resetRoundingButton click
  -> resetRoundingParameters
    -> resetParameterInputs
- resetOctavesButton click
  -> resetOctaveProgressionParameters
    -> resetParameterInputs
- resetPhaseButton click
  -> resetPhaseParameters
    -> resetParameterInputs
- resetParameterInputs
  -> updateParameterOutput
  -> rerenderErosionFromControls

Buffer-selection roots:
- each bufferButtons click event
  -> selectBuffer
    -> findBufferView
    -> updateBufferSelection
    -> drawSelectedBufferPreview
      -> findBufferView
      -> setCanvasSize

Download roots:
- each downloadTargets button click
  -> downloadRenderedOutput
    -> cleanFileStem
    -> downloadCanvasPng
      -> setProgress, if canvas.toBlob fails

Choose-file root:
- chooseButton click
  -> fileInput.click, browser API

Function families suggested by the DAG:
1. DOM references, configuration constants, grouped input collections, and appState.
2. Status, diagnostics, and buffer-preview navigation.
3. General math, image, canvas, and parameter-formatting utilities.
4. Surface preview and simple terrain-render drawing utilities.
5. Relaxed B-spline surface reconstruction.
6. WebGL Gaussian scale-space surface reconstruction.
7. Reconstruction method dispatch.
8. Shader source blocks and WebGL program/texture helpers.
9. Erosion shader execution and output readback.
10. Pipeline state, fitting/render orchestration, and diagnostics formatting.
11. Reset, download, and event-wiring roots.
